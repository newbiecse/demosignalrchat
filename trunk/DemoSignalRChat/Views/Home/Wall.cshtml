
@{
    Layout = null;
}

@using DemoSignalRChat.ViewModels
@model UserViewModel

@if (Request.IsAuthenticated)
{
    Html.ActionLink("aaaaaaaa", "Index", "Wall", new { id = "btn-wall" });
}